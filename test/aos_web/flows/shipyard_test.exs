defmodule AosWeb.ShipyardTest do
  use AosWeb.ConnCase, async: true

  describe "shipyard flow" do
    @tag :skip
    test "company hires an agent in a port" do
    end

    @tag :skip
    test "company is able to take an action that requires an agent" do
    end
  end

  describe "shipyard failure cases" do
    @tag :skip
    test "company tries to hire an agent when it already has one" do
    end

    @tag :skip
    test "company tries to take an action that requires an agent" do
    end
  end
end
